name: motivacional
description: Una aplicación de frases motivacionales con autenticación Firebase.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 Firebase
  firebase_core: ^3.4.0
  firebase_auth: ^5.2.0
  cloud_firestore: ^5.2.0
  firebase_storage: ^12.1.0

  # 💬 Notificaciones y mensajes cortos
  fluttertoast: ^8.2.6

  # 💡 Estado y utilidades
  provider: ^6.1.2

  # 🎨 UI y efectos visuales
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  lottie: ^3.1.2

  # 📤 Compartir frases
  share_plus: ^10.0.0

  # 🌍 Internacionalización
  intl: ^0.19.0

  # 📷 Manejo de imágenes
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1

  # 💾 Almacenamiento local
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/fonts/

  fonts:
    - family: Poppins
      fonts:
        - asset: assets/fonts/Poppins-Regular.ttf
        - asset: assets/fonts/Poppins-Bold.ttf
          weight: 700
